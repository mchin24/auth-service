{
    "default": {
        "paths": ["tests/features/*.feature"],
        "import": ["tests/features/step_definitions/*.ts"],
        "formatOptions": {
            "snippetInterface": "synchronous"
        }
    }
}